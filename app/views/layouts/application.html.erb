<!DOCTYPE html>
<html>
<head>
  <title>Office</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= javascript_importmap_tags %>
  <style>
    .nav a{
        text-decoration: none;
        color: white;
    }
    .hobar a{
        border: none;
        background-color: #942a1d;
        padding: 15px 20px;
        border-radius: 5px;
        border: none;
    }
    .hobar a:hover{
        margin-right: 10px;
        background:blue;
        padding: 15px 20px;
        border-radius: 5px;
        border: none;
        color: white;
    }
    .hobar {
        border: none;
        background: none;
    }
  </style>
</head>
<body>
<nav class="nav">
  <div style="margin-top: 30px;">
    <% if user_signed_in? %>
    <button class="hobar"><%= link_to 'Posts', posts_path %></button>
    <button class="hobar"><%= link_to 'New Post', new_post_path%></button>
    <button class="hobar"><%= link_to 'Logout', destroy_user_session_path, method: :delete %></button>
    <% else %>
      <button class="hobar"><%= link_to 'HOME', root_path%></button>
      <button class="hobar"> <%= link_to 'Login', new_user_session_path %></button>
      <button class="hobar"> <%= link_to 'Register', new_user_registration_path %></button>
    <% end %>
  </div>
</nav>
<main>
  <%= yield %>
</main>
</body>
</html>
